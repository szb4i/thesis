\chapter{Python Dependencies}
\label{appendix:a}

This appendix contains a list of all Python libraries used in the thesis.

\begin{itemize}
    \item Python \cite{rossum2009python}
    \item Matplotlib \cite{hunter2007matplotlib}: generating plots
    \item NumPy \cite{harris2020array}: storing array data structures, several data manipulation methods
    \item SciPy \cite{2020SciPy-NMeth}: convolving images with kernels
	\item scikit-image \cite{scikit-image}: applying \gls{clahe}
	\item scikit-learn \cite{scikit-learn}: applying \gls{pca}
\end{itemize}

\chapter{Code Snippets}
\label{appendix:b}

\captionsetup{justification   = raggedright,
              singlelinecheck = false}

This appendix contains code snippets used in this thesis.

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
img = read_image(...)
gamma = ...
img_transformed = img**0.2
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Power law transformation}
\label{code:power-law}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import cv2
import numpy as np
img = cv2.imread(...)
hist,bins = np.histogram(img.flatten(),256,[0,256])
cdf = hist.cumsum()
cdf_normalized = cdf * hist.max()/ cdf.max()
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Histogram of an image}
\label{code:histogram}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
cdf_m = np.ma.masked_equal(cdf,0)
cdf_m = (cdf_m - cdf_m.min())*255/(cdf_m.max()-cdf_m.min())
cdf = np.ma.filled(cdf_m,0).astype('uint8')
img2 = cdf[img]
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Equalizing histogram. Continuation of \ref{code:histogram}}	\label{code:histogram-equalized}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np
import cv2
img = cv2.imread(...)
clahe = cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8,8))
cl1 = clahe.apply(img)
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{CLAHE}
\label{code:clahe}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np
def get_gaussian_kernel(l=3, sig=1., normalize=True):
    ax = np.linspace(-(l - 1) / 2., (l - 1) / 2., l)
    gauss = np.exp(-0.5 * np.square(ax) / np.square(sig))
    kernel = np.outer(gauss, gauss)
    if normalize:
        return kernel / np.sum(kernel)
    return kernel
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Definition of \emph{get\textunderscore gaussian\textunderscore kernel}}
\label{code:get-gaussian-kernel}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
from scipy.signal import convolve2d
from utils.kernels.gaussian_kernel import get_gaussian_kernel
img = read_image(...)
kernel = get_gaussian_kernel(sig=5)
img_filtered_1 = convolve2d(img, kernel, mode='same')
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Convolution with low-pass Gaussian kernel. \emph{get\textunderscore gaussian\textunderscore kernel} can be found in Code Snippet \ref{code:get-gaussian-kernel}}
\label{code:gaussian-low-pass}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np	
def get_sobel_x_kernel():
  return np.array([[-1,0,1],[-2,0,2],[-1,0,1]], dtype=np.float)
def get_sobel_y_kernel():
  return np.array([[-1,-2,-1],[0,0,0],[1,2,1]], dtype=np.float)
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Definitions for Sobel kernels}
\label{code:sobel-kernels}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np
from scipy.signal import convolve2d
from utils.kernels.sobel_kernels import get_sobel_x_kernel, get_sobel_y_kernel
img = read_image(...)
kernel_x = get_sobel_x_kernel()
kernel_y = get_sobel_y_kernel()
gx = convolve2d(img, kernel_x, mode='same', boundary = 'symm', fillvalue=0)
gy = convolve2d(img, kernel_y, mode='same', boundary = 'symm', fillvalue=0)
magnitude = np.hypot(gx, gy)
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Sobel magnitude image. \emph{get\textunderscore sobel\textunderscore x\textunderscore kernel} and \emph{get\textunderscore sobel\textunderscore y\textunderscore kernel} can be found in Code Snippet \ref{code:sobel-kernels}}
\label{code:sobel-magnitude}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np
def get_laplacian_kernel():
  return np.array([[-1,-1,-1],[-1,8,-1],[-1,-1,-1]], dtype=float)
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Laplacian kernel}
\label{code:laplacian-kernel}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
from scipy.signal import convolve2d
from utils.kernels.laplacian_kernel import get_laplacian_kernel
img = read_image(...)
kernel = get_laplacian_kernel()
img_laplacian = convolve2d(
	img, kernel, mode='same', boundary = 'symm', fillvalue=0
)
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Convolution with Laplacian}
\label{code:laplacian-filtered}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np
img = read_image(...)
C = 9*np.identity(9)+np.full((9,9), -1)
normalizing_const = 1/(9**2)
kernel_dx = 1
kernel_dy = 1
img_wl = np.zeros((img.shape[0], img.shape[1]))
for x in range(kernel_dy, img.shape[1] - kernel_dy):
	for y in range(kernel_dx, img.shape[0] - kernel_dx):
		region_flatten = img[
			x - kernel_dx: x + kernel_dx + 1,
			y - kernel_dy: y + kernel_dy + 1
		].flatten()
		img_wl[x,y] = normalizing_const*(
			region_flatten.dot(C.dot(region_flatten))
		)
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{WL operator}
\label{code:wl}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np		
img = read_image(...)
img_ft = np.fft.fft2(img)
img_ft_abs = np.log(np.abs(img_ft))
img_ft_abs_shifted = np.fft.fftshift(img_ft_abs)
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Visualization of the Fourier transform. Variable \emph{img\textunderscore ft} is the discrete Fourier transform of the image. In the next line, the absolute value of the image is taken as the result of the Fourier transform outputs complex values. Furthermore, logarithm function needs to be applied in order to see any information on the output. Otherwise, the output is completely dark. This stage is variable \emph{img\textunderscore ft\textunderscore abs}. The default location of the zero frequency is in the upper left corner of the image rather than its center. This is not a mistake, it is how numpy handles the ordering of frequencies. Shifting has to be called explicitly if wanted.}
\label{code:fft-shift}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np		
img = read_image(...)
u = np.fft.fftfreq(img.shape[1])
v = np.fft.fftfreq(img.shape[0])
vv, uu = np.meshgrid(v, u, indexing='ij')
D_0 = 0.05
filter_f = (np.hypot(vv, uu) < D_0)
img_f = np.fft.fft2(img)
img_filtered = np.fft.ifft2(img_ft*filter_f).real
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Implementation and application of \gls{ilpf} (Equation \ref{eq:ilpf}). First, a coordinate system is generated with the discrete Fourier transform sample frequencies $u$ and $v$ matching the shape of the input image. Then, the function $np.meshgrid$ creates coordinate arrays for the $v$ and the $u$ coordinates and saves them into variable $vv$ and $uu$. Finally, \gls{ilpf} is calculated in frequency domain and stored in variable \emph{filter\textunderscore f}.}
\label{code:ilpf}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np		
img = read_image(...)
v = np.fft.fftfreq(img.shape[0])
u = np.fft.fftfreq(img.shape[1])
vv, uu = np.meshgrid(v, u, indexing='ij')
sigma = 1/10
filter_f = np.exp(-np.hypot(uu, vv)**2/(2*sigma**2))
img_f = np.fft.fft2(img)
img_filtered = np.fft.ifft2(img_f*filter_f).real
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Implementation of \gls{glpf} (Equation \ref{eq:glpf})}
\label{code:glpf}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np		
img = read_image(...)
v = np.fft.fftfreq(img.shape[0])
u = np.fft.fftfreq(img.shape[1])
vv, uu = np.meshgrid(v, u, indexing='ij')
sigma = 1/10
filter_f = 1 - np.exp(-np.hypot(uu, vv)**2/(2*sigma**2))
img_f = np.fft.fft2(img)
img_filtered = np.fft.ifft2(img_f*filter_f).real
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Implementation of \gls{ghpf} (Equation \ref{eq:ghpf})}
\label{code:ghpf}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np		
img = read_image(...)
v = np.fft.fftfreq(img.shape[0])
u = np.fft.fftfreq(img.shape[1])
vv, uu = np.meshgrid(v, u, indexing='ij')
sigma = 1/10
filter_f = 1 - np.exp(-np.hypot(uu, vv)**2/(2*sigma**2))
img_f = np.fft.fft2(img)
k = 1
img_filtered = np.fft.ifft2(img_f*(k+filter_f)).real
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Implementation of \gls{ghpf} with DC term (Equation \ref{eq:ghpf-plus-dc}). By changing $k$ (last two lines), one can control the contribution of the DC term}
\label{code:ghpf-plus-dc}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np		
img_f = np.fft.fft2(img)
v = np.fft.fftfreq(img.shape[0])
u = np.fft.fftfreq(img.shape[1])
vv, uu = np.meshgrid(v, u, indexing='ij')
sigma_1 = 0.15
gauss_1 = np.exp(-(np.hypot(uu, vv))**2/(2*sigma_1**2))
sigma_2 = 0.16
gauss_2 = np.exp(-(np.hypot(uu, vv))**2/(2*sigma_2**2))
gauss_diff = gauss_2 - gauss_1
img_filtered = np.fft.ifft2(img_f * gauss_diff).real
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Implementation of a band-pass filter (Equation \ref{eq:gauss-diff})}
\label{code:gauss-diff}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np		
img_f = np.fft.fft2(img)
v = np.fft.fftfreq(img.shape[0])
u = np.fft.fftfreq(img.shape[1])
vv, uu = np.meshgrid(v, u, indexing='ij')
C0 = 0.25
W = 0.1
filter_f = np.exp(-(np.hypot(uu, vv) - C0)**2/(W**2))
img_filtered = np.fft.ifft2(img_f * filter_f).real
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Implementation of a band-pass filter (Equation \ref{eq:gauss-alternative}). $C_{0}$ is the center of the band and $W$ is the width of the band}
\label{code:bandpass-gauss-alternative}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
from skimage import exposure
img_read_image(...)
img_clahe = return exposure.equalize_adapthist(
	img_input,kernel_size=[img_input.shape[0]/10,
	img_input.shape[1]/10],clip_limit=0.00015,nbins=26200
)
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{CLAHE with scikit-image}
\label{code:clahe-scikit}
\end{figure}


\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np
from scipy.signal import convolve2d
from utils.kernels.sobel_kernels import get_sobel_x_kernel, get_sobel_y_kernel
img = read_image(...)
kernel_x = get_sobel_x_kernel()
kernel_y = get_sobel_y_kernel()
img_gradient_x = convolve2d(
	img, kernel_sobel_x, mode='same',
	boundary = 'symm', fillvalue=0
)
img_gradient_y = convolve2d(
	img, kernel_sobel_y, mode='same',
	boundary = 'symm', fillvalue=0
)
img_gradient_direction = np.arctan2(img_gradient_y, img_gradient_x)
img_gradient_direction_gradient_x = convolve2d(
	img_gradient_direction, kernel_sobel_x, mode='same',
	boundary = 'symm', fillvalue=0
)
img_gradient_direction_gradient_y = convolve2d(
	img_gradient_direction, kernel_sobel_y, mode='same',
	boundary = 'symm', fillvalue=0
)
img_gradient_direction_gradient_mangitude = np.hypot(
	img_gradient_direction_gradient_x,
	img_gradient_direction_gradient_y
)
# inversion: where directional change is small -> veins
# where directional change is big -> noise
# inverting to have strong signal where veins are
img_gradient_direction_gradient_mangitude_inverted = np.log(
	1/(img_gradient_direction_gradient_mangitude+0.0001)
)
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{Magnitude of directional changes. \emph{get\textunderscore sobel\textunderscore x\textunderscore kernel} and \emph{get\textunderscore sobel\textunderscore y\textunderscore kernel} can be found in Code Snippet \ref{code:sobel-kernels}}
\label{code:smart-sobel}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np
from skimage.filters import frangi
from skimage import morphology
from sklearn.decomposition import PCA
def get_pca_filtered_image(img, threshold_variance_ratio):
  pca = PCA(n_components=img.shape[1])
  transformed = pca.fit_transform(img)
  transformed_copy = transformed.copy()
  for i, variance_ratio in enumerate(pca.explained_variance_ratio_):
    if variance_ratio < threshold_variance_ratio:
      transformed_copy[:, i:] = 0
      break
  img_pca_filtered = pca.inverse_transform(transformed_copy)

  return img_pca_filtered
# read in whole sequence of x-ray frames
img_stack = ...
img_dva = np.std(img_stack, axis=2, ddof=1)
img_frangi = frangi(img_dva, black_ridges=True)

### opening: eliminates single pixels (noise removal)
square = morphology.square(width=2)
img_morphological_opening = morphology.dilation(
	morphology.erosion(img_frangi, square)
)
img_morphological_opening[0.15 > img_morphological_opening] = 0
img_morphological_opening[0.15 < img_morphological_opening] = 1
img_morphological_opening = img_morphological_opening.astype(bool)
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{\gls{pca} filtering part 1}
\label{code:pca1}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
img_stack_flatten = np.reshape(
	img_stack,
	(img_stack.shape[0]*img_stack.shape[1], img_stack.shape[2])
)
img_pca_filtered_vein_domain = get_pca_filtered_image(
	img_stack_flatten[img_morphological_opening.flatten()],
	1e-05
)
img_pca_filtered_background_domain = get_pca_filtered_image(
	img_stack_flatten[~img_morphological_opening.flatten()], 
	5e-05
)
img_stack_filtered = np.zeros((
	img_stack.shape[0],
	img_stack.shape[1],
	img_stack.shape[2]
))
for k in range(0, img_stack.shape[2]):
  index_vein_domain = 0
  index_background_domain = 0
  for i in range(0, img_stack.shape[0]):
    for j in range(0, img_stack.shape[1]):
      if img_morphological_opening[i,j]:
        img_stack_filtered[i,j,k] = img_pca_filtered_vein_domain[
			index_vein_domain,
			k
		]
        index_vein_domain += 1
      else:
        img_stack_filtered[i,j,k] = img_pca_filtered_background_domain[
			index_background_domain,
			k
		]
        index_background_domain += 1
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{\gls{pca} filtering part 2}
\label{code:pca2}
\end{figure}

\begin{figure}[htpb]
	\begin{tabular}{c}
	\begin{lstlisting}[language=Python]
import numpy as np
W = 0.15
C0 = 0.25
sum_weight = 5
img_f = np.fft.fft2(img)
v = np.fft.fftfreq(img.shape[0])
u = np.fft.fftfreq(img.shape[1])
vv, uu = np.meshgrid(v, u, indexing='ij')
filter_f = np.exp(-(np.hypot(uu, vv) - C0)**2/(W**2))
img_filtered = np.fft.ifft2(img_f * filter_f).real
img_final = img + sum_weight*img_filtered
img_final = get_clahe_img(scale(img_final))
	\end{lstlisting}
	\end{tabular}
	\caption[Example listing]{The choesen algorithm derived from \ref{code:bandpass-gauss-alternative}. CLAHE function is based on \ref{code:clahe-scikit}}
\label{code:chosen-algorithm}
\end{figure}


